name: merge_blocks
description: Merge Blocks Neon Chain
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # Persistence (kalıcı kayıt)
  shared_preferences: ^2.2.3

  # iOS icons font (Flutter template standard)
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
